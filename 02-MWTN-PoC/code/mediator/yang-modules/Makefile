##################### LIBRARIES ########################
#
# DIRS:  
# This is an ordered list
# Put only the directories that build object files in the list

# make all targets
DIRS = SimpleAirInterface-ObjectClasses \
       MicrowaveModel-ObjectClasses-MwConnection \
       CoreModel-CoreNetworkModule-ObjectClasses

#################### PLATFORM DEFINITIONS ############

#

################################ MAKE RULES #################################

all: moduleall

clean: moduleclean

install: moduleinstall

moduleall:
	for dir in $(DIRS); do\
	  cd $$dir && $(MAKE) && cd ..;\
        done

moduleclean:
	for dir in $(DIRS); do\
	  cd $$dir && $(MAKE) clean && cd ..;\
        done

moduleinstall:
	for dir in $(DIRS); do\
          cd $$dir && $(MAKE) install && cd ..;\
        done


.PHONY: all clean superclean install \
	moduleall moduleclean moduleinstall 


# prevent the make program from choking on all the symbols
# that get generated from autogenerated make rules
.NOEXPORT:



