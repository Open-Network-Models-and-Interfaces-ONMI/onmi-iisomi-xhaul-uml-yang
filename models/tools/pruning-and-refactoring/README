#pruning-and-refactoring
this is the place where the configuration of different UML modules take place, so that these modules are converted into YANG files through the 
  automated uml2yang converter.

##Create a new Module
pull the latest version from github (experimental) if avaialble
   git status
   git pull origin experimental
##create a new directory
  $ cd models/tolls/pruning-and-refactoring
  $ mkdir module-name
    Note: Remember that the naming convenstion is important. e.g. IpInterface => ip-interface
  $ cd <module-name>
  $ mkdir –R src/main/resources
  
##add the newly created directory in pom.xml
   <modules>
     <module> module-name</module>
   </modules>

##Add UML file and the related resources
  $ cd models/tools/pruning-and-refactoring/hybrid-mw-structure/src/main/resources
  $ mkdir <UMLFileName> e.g. “HybridMwStructure” 
  $ cd models/tools/pruning-and-refactoring/hybrid-mw-structure/src/main/resources/<UMLFileName>
  past the following files and directories here. 
   - CommonDataTyes
   - Gendoc
   - UmlProfiles
   - <UMLFileName>.di 
   - <UMLFileName>-notation
   - <UMLFileName>.uml
  ##Add a local pom.xml file
  $ cd models/tools/pruning-and-refactoring/<uml-module>/src/main/resources
  create a pom.xml file  (an existing local pom file from another module can be copied and edited)
  
  
  - unzip the file HybridMwStructure_2.0.0-tsp.190613.1830.zip under “/hybrid-mw-structure/src/main/resources”
- Add resource path and the xslt in the pom file 
- go to module directory
 cd  /5G-xHaul/models/tools/pruning-and-refactoring/hybrid-mw-structure
- edit the pom.xml file
- under <transformationSet> add the path of 
     -> resource directory
     -> xslt directory
    e.g. 
   <dir>${basedir}/src/main/resources/HybridMwStructure</dir>
   <stylesheet>${basedir}/src/main/resources/xslt/prune-and-refactor-hybrid-mw-structure.xslt</stylesheet>
 5) convert UML to YANG 
- go to the tools directory "/5G-xHaul/models/tools"
 - execute "mvn clean install"
 if the complilation is successfull, find the the output under /5G-xHaul/models/tools/xmi-to-yang/target/yang
